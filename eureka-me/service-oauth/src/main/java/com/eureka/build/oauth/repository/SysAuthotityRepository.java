package com.eureka.build.oauth.repository;


import com.eureka.build.oauth.domain.SysAuthority;
import com.eureka.build.oauth.repository.support.WiselyRepository;

public interface SysAuthotityRepository
        extends WiselyRepository<SysAuthority,Long> {
}
